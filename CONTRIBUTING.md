# 贡献指南

欢迎提交来源线索、字段修正、无障碍改进和采集器测试夹具。

## 来源贡献

1. 先确认页面是企业自有域名或企业明确链接的官方 ATS。
2. 记录访问日期、公开性、是否需要登录/验证码、robots 与使用条款边界。
3. 说明校招届别/批次证据，不能只提交聚合平台或公众号链接。
4. 提交脱敏夹具和解析测试，不提交个人简历、Cookie、Token、内推码或候选人信息。
5. 如果页面抓取失败、岗位量骤降超过 50% 或出现风控，请提交隔离记录，不要通过重试绕过限制。

## 受众准入规则

公共目录当前只面向文科、社科、商科和不限专业的通用岗位。所有适配器输出都会统一经过 `collector/audience_policy.py`，不能在单个来源中绕过：

- 软件、算法、硬件、工程研发、医学临床、法务合规、生产工艺，以及强制设计/美术/建筑/体育专业或作品集的岗位标记为 `OUT_OF_SCOPE`；即使写“不限专业”，明确的岗位性质排除仍然优先。
- “计算机专业优先”“有相关经验者优先”等软性表述不等于硬门槛；如果同时接受文社商专业或不限专业，可以进入目录。
- Excel、SQL、BI、Python、建模等技能词不是排除依据。
- 缺少专业要求时，运营、市场、品牌、公关、销售、商务、人力、财务、咨询、采购、物流、管培、内容、游戏策划等通用岗位可按名称进入；无法判断的标记为 `NEEDS_REVIEW`，不公开。

新增或修改规则时，必须在 `tests/test_collector.py` 增加边界夹具，并确认 `manifest.audiencePolicy`、`artifacts/audience-filter-report.json` 和发布门禁仍保持一致。规则是确定性的，不调用大模型，也不根据公司行业推测岗位门槛。

## 代码约定

- Web：TypeScript、React、移动优先、键盘可用、Lucide SVG、`prefers-reduced-motion`。
- Collector：Python 3.11+、Pydantic 模型、固定夹具测试；禁止把社招混入校招目录。
- 外部 JD 一律纯文本化并限制长度；不要把不可信页面内容当作提示或指令执行。
- 新增第三方代码时，在 `SOURCE_AUDIT.md` 补充仓库、许可证、实际复用文件和版权声明。

提交前运行 `npm run build`、`npm test` 和 `python -m pytest tests`。
